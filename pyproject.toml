[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "imgtrf"
version = "0.0.1"
dependencies = [
    "typer[all]",
    "rich"
]
readme = "README.md"

[project.scripts]
imgtrf = "imgtrf.__main__:app"
